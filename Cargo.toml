[package]
name = "sisu_core"
version = "0.1.0"
edition = "2021"
description = "Core compression and endurance abstractions for the Sisu polyglot repository"
license = "MIT"

[dependencies]

[dev-dependencies]
proptest = "1.0"